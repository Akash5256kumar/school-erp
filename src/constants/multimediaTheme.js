import colors from "./colors";

export const multimediaTheme = {
  palette: {
    headerGradient: [colors.primary, colors.info],
    iconAccent: colors.info,
    iconSurface: colors.surfaceMuted,
    screenGradient: ["#DFE6FF", colors.surface],
  },
};

export default multimediaTheme;
